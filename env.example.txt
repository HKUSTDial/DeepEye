# ==================================================
# DeepEye Environment Configuration
# ==================================================
# 复制此文件为 .env 并修改配置

# ----- Service Ports -----
HOST_BACKEND_PORT=8000
HOST_FRONTEND_PORT=5173
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# ----- Database Configuration -----
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=deepeye
POSTGRES_STATE_DB=deepeye_state

# ----- Redis Configuration -----
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# ----- MinIO Configuration (Sandbox Storage) -----
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_SANDBOX_BUCKET=deepeye-sandboxes

# ----- Sandbox Configuration -----
SANDBOX_TYPE=docker
SANDBOX_IMAGE=deepeye-sandbox:latest
SANDBOX_DOCKERFILE=docker/Dockerfile.sandbox
SANDBOX_AUTO_BUILD=true

# Sandbox Lifecycle Management
SANDBOX_IDLE_TIMEOUT=1800          # 30 minutes - stop container
SANDBOX_ARCHIVE_TIMEOUT=7200       # 2 hours - backup and destroy
SANDBOX_CLEANUP_INTERVAL=300       # 5 minutes - check interval
SANDBOX_AUTO_RESTORE=true          # Auto restore from backup

# ----- LLM Configuration (REQUIRED) -----
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4
LLM_TEMPERATURE=0.7

# ----- E2B Configuration (Optional) -----
# E2B_API_KEY=your_e2b_api_key

# ----- Daytona Configuration (Optional) -----
# DAYTONA_WORKSPACE_URL=your_daytona_url







